import React, { Fragment } from "react";
import Machines from "./Machines";

export default function Dashboard() {
    return (
        <Fragment>
            <Machines />
        </Fragment>
    );
}